#Para definir un vector
w <- c(1,2,3)

# Pera definir un vector constante r = (a,a,a,a...)
v <- rep(pi,7) 

#definir un vector por consola
k <- scan()

#Modificar en un editor de texto un vector
fix(k)

nombre <- c('Santiago','Maria','Juan','Rosita')
class(nombre)

length(nombre)

?scan

anio <- rep(2000, 10)
anio

entradas <- scan()
fix(entradas)

## leer caracteres o strings
scan(what = 'character')
